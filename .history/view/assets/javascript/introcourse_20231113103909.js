toggle = () => {
  const listPanelCourse = document.querySelector(".list-panels-course");
  const listTabPanel = document.querySelector(".list-tab-panel");

  listPanelCourse.target.addEventListener("click", toggleMenu => {
    if (listPanelCourse.target)
  }
};
